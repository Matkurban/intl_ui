import 'package:flutter/material.dart';

class IntlBackButton extends StatelessWidget {
  const IntlBackButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('IntlBackButton'));
  }
}
