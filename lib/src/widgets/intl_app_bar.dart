import 'package:flutter/material.dart';

class IntlAppBar extends StatelessWidget {
  const IntlAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('IntlAppBar'),
    );
  }
}
