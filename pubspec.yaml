name: intl_ui
description: "A new Flutter project."
version: 0.0.1
homepage: https://github.com/Matkurban/intl_ui

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

