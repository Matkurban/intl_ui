name: intl_ui
description: "A Flutter UI package that provides internationalized widgets and components for building multilingual applications."
version: 0.0.4
homepage: https://github.com/Matkurban/intl_ui

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

